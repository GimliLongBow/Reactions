<h1>Thanks for submitting your review!</h1>

<p>We'd love to hear more about why you rated <%= @user.name %> this way. Please?</p>

<%= simple_form_for(@review) do |f| %>
  <%= f.input :comment %>
  <%= f.input :user_id, collection: User.all, label_method: :name, prompt: 'Select a user' %>

  <div class="pull-right">
    <%= f.button :submit, 'Send feedback!', class: 'btn btn-success' %>
  </div>
<% end %>
